const initialState = {
    test: null
}

export default (state = initialState, {type, payload}) => {
  switch (type) {

//   case typeName:
//     return { ...state };

  default:
    return state
  }
};
